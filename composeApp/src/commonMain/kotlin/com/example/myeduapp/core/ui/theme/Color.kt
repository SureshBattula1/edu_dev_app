package com.example.myeduapp.core.ui.theme

import androidx.compose.ui.graphics.Color

// Sunrise Academic Design System - Color Palette
val PrimaryBlue = Color(0xFF007CC4)      // Vibrant Academic Blue
val PrimaryLight = Color(0xFF299EF2)     // Lighter Vibrant Blue
val SecondaryBlue = Color(0xFFE8F2FF)    // Light Ice Sky Blue (Secondary Container)
val SecondaryBlueVariant = Color(0xFFF0F4FC)

// Surfaces
val Background = Color(0xFFF8F9FF)       // Clean, crisp off-white canvas
val CardBackground = Color(0xFFFFFFFF)   // Pure white for cards and modals
val SurfaceVariant = Color(0xFFE8F2FF)
val OutlineSoft = Color(0xFFE2E8F0)      // Subtle 1px border color

// Typography
val PrimaryText = Color(0xFF0F172A)      // Deep Navy / Slate 900
val SecondaryText = Color(0xFF64748B)    // Muted Slate 500
val OnPrimaryText = Color(0xFFFFFFFF)    // Crisp white for text on primary blue

// Status & Accents
val SuccessColor = Color(0xFF10B981)     // Emerald Green (Active status)
val InfoColor = Color(0xFF0369A1)        // Faculty tag text
val BadgeBackground = Color(0xFFE0F2FE)  // Faculty tag background
val WarningColor = Color(0xFFF59E0B)     // Amber
val ErrorColor = Color(0xFFEF4444)       // Red
